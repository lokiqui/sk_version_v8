import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'authentication.starter.template',
  appName: 'authentication-starter-template',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
